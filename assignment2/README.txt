How to run:
Source the script and then use the functions as commands


---- track.sh ----
Assumption: The user has a permanent environment variable in their .bashrc or .zshrc file called LOGFILE name for the file to log tasks

The date command works with the current options on MacOS, but I have not been able to test on IFI machines.
